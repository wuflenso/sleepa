# MySQL
#   gem install mysql2
#
#   Ensure the mysql2 gem is defined in your Gemfile
#   gem "mysql2"
#
default: &default
  adapter: mysql2
  encoding: utf8

development:
  <<: *default
  database: sleepa_dev
  username: <%= ENV.fetch("MYSQL_USERNAME_DEV") { 'sleepa' } %>
  password: <%= ENV.fetch("MYSQL_PASSWORD_DEV") { 'sleepa' } %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
  host: <%= ENV.fetch('MYSQL_HOST_DEV') { '127.0.0.1' } %>
  port: <%= ENV.fetch('MYSQL_PORT_DEV') { '3307' } %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  database: ":memory:"


# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
production:
  primary:
    <<: *default
    database: sleepa_production
    username: <%= ENV.fetch("MYSQL_USERNAME") { 'sleepa' } %>
    password: <%= ENV.fetch("MYSQL_PASSWORD") { 'sleepa' } %>
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
    host: <%= ENV.fetch('MYSQL_HOST') { '127.0.0.1' } %>
    port: <%= ENV.fetch('MYSQL_PORT') { '3307' } %>
